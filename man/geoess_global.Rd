% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/global_ess.R
\name{geoess_global}
\alias{geoess_global}
\title{Global ESS as an Expectation of Local ESS}
\usage{
geoess_global(
  Q,
  I1,
  Ipi,
  method = c("trace", "dir", "eig"),
  v = NULL,
  ess_fun = NULL,
  psd = c("none", "clip", "nearPD"),
  nsim = 5000,
  seed = 1,
  weights = NULL,
  mixture_attribution = c("hard_resp", "mix", "weight", "responsibility", "none"),
  return_samples = FALSE,
  eig_summary = c("mean", "quantiles"),
  eig_probs = c(0.05, 0.5, 0.95),
  ...
)
}
\arguments{
\item{Q}{Distribution for \eqn{\theta}. Can be a draws matrix, list with
\code{draws} and optional \code{weights}, a function \code{rQ(n)},
or a \code{geoess_mix} object.}

\item{I1}{Fisher information per observation. Matrix, function, or list
with \code{type = "hessian"}.}

\item{Ipi}{Prior curvature. Matrix, function, \code{geoess_mix}, or list
with \code{type = "hessian"}.}

\item{method}{Character. "trace", "dir", or "eig".}

\item{v}{Direction for \code{method = "dir"}.}

\item{ess_fun}{Optional custom scalarization \code{ess_fun(I1, Ipi, theta)}.}

\item{psd}{Character. "none", "clip", or "nearPD" to enforce PSD on Ipi.}

\item{nsim}{Integer. Number of draws when \code{Q} is a generator or mixture.}

\item{seed}{Integer seed for reproducibility when sampling.}

\item{weights}{Optional weights aligned with draws.}

\item{mixture_attribution}{Character. When \code{Ipi} is a mixture prior,
controls how pointwise local ESS is formed before averaging. The default
\code{"hard_resp"} uses the weighted hard-assignment rule:
\eqn{w_{k^*}\,\mathrm{ESS}_k(\theta)} for a unique max-responsibility
component, with ties averaged across the tied components. \code{"mix"}
uses full-mixture curvature \eqn{-\nabla^2 \log \sum_k w_k \pi_k(\theta)}.
The options \code{"weight"}, \code{"responsibility"}, and \code{"none"}
use \eqn{\sum_k s_k(\theta) I_{\pi_k}(\theta)} with
\eqn{s_k(\theta) = w_k}, \eqn{r_k(\theta)}, and \eqn{1}, respectively.}

\item{return_samples}{Logical; if TRUE return local ESS samples.}

\item{eig_summary}{Character. "mean" or "quantiles" for eigen summaries.}

\item{eig_probs}{Numeric vector of quantile probabilities.}

\item{...}{Passed to log-likelihood or log-prior functions.}
}
\value{
A \code{geoess_global_result} object.
}
\description{
Computes global ESS as an expectation of local ESS values under a weighting
distribution \eqn{Q(\theta)}.
}
